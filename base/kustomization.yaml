apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: sandbox-app-values-base
    files:
      - values.yaml=common-values.yaml
generatorOptions:
  disableNameSuffixHash: true