apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dev
configMapGenerator:
  - name: sandbox-app-values
    files:
      - sandbox-app.yaml
generatorOptions:
  disableNameSuffixHash: true
