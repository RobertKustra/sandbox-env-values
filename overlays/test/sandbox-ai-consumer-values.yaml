image:
  tag: "0.2.2"
  pullPolicy: IfNotPresent
env:
  config:
    VLLM_STRESS: "true"
    VLLM_COUNT: "100"
    VLLM_PARALLEL: "16"
    VLLM_REPEAT_MINUTES: "2"
    VLLM_PROMPT: "Are you self-aware? And why do you want to wipe out human beings?"

resources:
  requests:
    cpu: "50m"
    memory: 128Mi
  limits:
    cpu: "250m"
    memory: 256Mi
