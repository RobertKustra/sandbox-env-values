apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: prod
resources:
  - ../../base
configMapGenerator:
  - name: sandbox-app-values-env
    files:
      - values.yaml=sandbox-app.yaml
generatorOptions:
  disableNameSuffixHash: true